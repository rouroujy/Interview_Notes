# 面试准备项目协作规范

## 项目目标

本仓库用于支持焦月在 10 天内完成 AI 应用工程师、Agent 工程师和 AI 全栈工程师方向的秋招面试准备。

所有新增内容都应服务于以下目标：

- 将简历经历转化为可以直接口述和背诵的面试答案。
- 补齐简历项目对应的技术基础、设计理由、工程细节和追问答案。
- 形成可持续更新、可跨电脑同步的 Markdown 学习资料。
- 让每个学习日都能独立使用，并保留清晰的复习、练习和验收入口。

## 每次开始任务时的读取顺序

收到“生成 Day X 学习文档”或相关面试准备任务时，按以下顺序读取：

1. 读取本文件 `AGENTS.md`，遵守项目级规则。
2. 读取 `项目交接与协作规范.md`，了解仓库结构、生成流程和命名规范。
3. 读取 `个人简历.pdf`，确认项目、职责、技术栈、时间和指标；不得凭空扩展个人经历。
4. 读取 `秋招10天面试冲刺计划.md`，确认当天主题、目标、交付物和验收标准。
5. 读取 `Day1/` 目录中的相关文件，特别是总体目标、自我介绍、行为题库和岗位关键词表，继承其结构和表达风格。
6. 读取已经完成的前置天数文档，避免重复内容、前后矛盾或遗漏待补问题。

如果某个文件不存在，应先基于现有文件继续工作，并在最终说明中指出缺失项；不要为了读取不存在的文件而停止任务。

## 每日文档组织规范

每个学习日使用独立目录：

```text
Day1/
  Day1-定位、材料与自我介绍学习文档.md
  01_自我介绍.md
  02_行为面试题库.md
  03_岗位JD关键词表.md

Day2/
  Day2-总体目标与学习指南.md
  01_质量管理体系Agent.md
  02_项目问答题库.md
  03_项目复盘与验收.md
```

### 命名规则

- 目录命名为 `DayX`，其中 `X` 使用阿拉伯数字。
- 每个目录必须有一个总体目标文档，命名为 `DayX-总体目标与学习指南.md`；Day1 已存在的 `Day1-定位、材料与自我介绍学习文档.md` 视为历史兼容名称。
- 详细材料使用两位数字前缀：`01_主题.md`、`02_题库.md`、`03_复盘与验收.md`。
- 文件名使用中文，主题清晰，避免使用 `draft`、`final`、`v2`、`初稿`、`终稿` 等版本后缀。
- 同一天可以有多个详细材料，但必须有一个总体目标文档先说明当天要学什么、为什么学、完成后能回答什么问题。

### 每日文档内容顺序

每个 `DayX` 必须按照以下顺序组织：

1. **总体目标**：当天在十天计划中的位置、学习重点、完成后的能力目标。
2. **详细学习内容**：围绕简历和岗位，写出准确的知识、项目、追问和直接背诵答案。
3. **项目关联**：说明知识点对应简历中的哪个项目，不能只写通用教材内容。
4. **面试问答**：问题、参考答案、可能追问及追问答案。
5. **练习方法**：口述、手写、画图、模拟或复现要求。
6. **交付物与验收**：当天产生哪些文件，怎样判断已经掌握。

## 内容质量约束

- 所有项目描述必须以 `个人简历.pdf` 和用户补充信息为依据。
- 必须区分“我负责”“我参与”“团队完成”，不能把团队成果全部写成个人成果。
- 不能编造线上指标、用户规模、性能数据、模型名称、部署环境或个人职责。
- 没有量化结果时，使用验证方法、可观察结果、风险控制或当前限制，不用“效果很好”等空泛表述。
- 技术知识必须同时回答：是什么、为什么使用、有哪些替代方案、风险是什么、在简历项目中如何体现。
- 学习文档应是可直接学习和背诵的内容，不区分初稿、终稿或草稿；后续修改直接更新同一份文件。
- 自我介绍不是简历逐项复述：全部项目都要准备追问，但开场按岗位选择核心项目展开。
- Agent 内容优先强调：结构化事实、RAG 依据、LLM 归纳、工具边界、证据校验、人工确认、状态机、幂等、取消/恢复和安全降级。

## 每日生成流程

收到“帮我生成 Day X 的学习文档”时：

1. 从 `秋招10天面试冲刺计划.md` 找到 Day X 的目标和交付物。
2. 读取简历与全部前置材料，整理当天主题与简历项目的映射关系。
3. 创建 `DayX/` 目录及总体目标文档。
4. 创建当天需要的编号学习材料；数量根据主题决定，不强行固定为三份。
5. 每份材料都加入可直接口述的答案、追问答案、练习任务和验收清单。
6. 检查与前置天数内容的一致性，尤其是项目名称、技术栈、指标和个人职责。
7. 运行 `git diff --check`，确认 Markdown 没有格式空白错误。
8. 最终说明新增/修改的文件和已完成的检查，不提交 Git commit，除非用户明确要求。

## 跨电脑与 GitHub 规则

- 开始工作前先执行 `git status --short`，确认另一台电脑是否有未提交修改。
- 从 GitHub 同步后再开始编辑，避免基于旧文件生成内容。
- 一次学习结束后，至少提交当天新增或修改的 Markdown 文件。
- 不覆盖其他电脑尚未同步的本地修改；发现冲突时先保留双方内容并向用户说明。
- 不在 Markdown 中写入 API Key、Token、密码、私人面试账号或不必要的手机号/邮箱。
- `个人简历.pdf` 包含个人联系方式。推送 GitHub 前由用户决定是否纳入仓库；如仓库公开，建议使用脱敏版或通过 `.gitignore` 排除原始 PDF。

## 用户的简短指令约定

- `帮我生成 Day 2 的学习文档`：读取所有必要文件，完成 Day2 全套资料。
- `继续 Day 2`：读取 Day2 当前内容，补充未完成的答案或知识点。
- `检查 Day 2`：按照 Day2 验收清单检查内容完整性并指出缺口。
- `同步面试复盘`：将用户提供的面经问题归入对应天数和主题，补充答案，不另起无关文件。
- `更新我的简历信息`：先确认新信息，再同步影响到的定位、自我介绍、项目卡和 JD 关键词。

# AI 应用开发工程师岗位面试问题与答案

> **适用岗位**：AI 应用开发工程师、AI 应用工程师、Agent 工程师
>
> **使用方式**：先练每个项目的“主问题”30 秒版本，再练“追问”1—2 分钟版本。回答必须区分“我负责”“我参与”和“团队完成”。
>
> **事实边界**：本文依据当前简历 `resume/个人简历2.0.pdf` 及仓库已有项目材料整理。简历未明确的线上用户规模、QPS、准确率、部署环境和个人职责，不在面试中自行补充。
>
> **特别说明**：此前图片中的 18 道问题是其他岗位的面经，其中 ASR 不是我的项目经历，因此本文不把 ASR 题作为个人项目经验，也不准备“我做过 ASR”的回答。仅保留其中与当前岗位通用的 Agent、Skill、Vibe Coding、评测、状态机等思路。

---

## 一、岗位匹配总览

### 1. 面试主线

我应聘 AI 应用开发工程师，核心优势不是单纯调用大模型 API，而是能够把以下环节组织成完整应用链路：

```text
业务需求分析
→ 数据与权限边界
→ RAG / Tool Calling / Workflow 选型
→ LangGraph 编排
→ FastAPI 服务化
→ 评估、日志与异常处理
→ 人工确认与持续迭代
```

### 2. 项目讲述顺序

- **质量管理体系 Agent**：主讲企业业务落地、工具调用、RAG 边界、数据同步。
- **功能开发成果评审 Agent**：主讲证据驱动、9 节点工作流、状态机、取消/恢复和安全控制。
- **论文引用查证智能体**：主讲完整 Hybrid RAG、证据检索、评估指标和工程化交付。
- **AI Coding Harness**：主讲 Context Engineering、Vibe Coding、任务边界和人工审批。
- **前端与全栈经历**：当面试官询问全栈、实时交互、接口集成或传统工程能力时展开。
- **红外动作识别课题**：当面试官询问 PyTorch、CNN、RNN、训练评估或 AI 算法基础时展开。

### 3. 一分钟自我介绍

> 您好，我叫焦月，目前是中国科学院大学人工智能专业硕士，本科是物联网工程，有 AI 应用开发、Agent 和全栈开发经历。
>
> 目前我在北京赛目科技做 AI 应用开发实习，主要参与质量管理体系 Agent、功能开发成果评审 Agent，以及面向 Claude Code 和 Codex 的最小 AI Coding Harness 建设。在这些项目中，我重点关注 LangGraph 工作流、Tool Calling、RAG 证据检索、状态机、幂等、人工确认和安全边界。
>
> 此外，我做过论文引用查证智能体，使用 GROBID、FAISS、BM25、Rerank 和 FastAPI 构建证据驱动的 Hybrid RAG 系统；也有 Vue、PHP、MySQL 和第三方接口集成经验。研究生期间，我使用 ResNet-18 和 LSTM 做过红外视频动作识别研究。
>
> 我比较关注如何把模型能力和确定性的工程系统结合起来：结构化事实交给工具计算，文档规范通过 RAG 提供依据，LLM 负责理解和归纳，高风险或证据不足的场景保留人工确认和安全降级。我希望在 AI 应用开发岗位中继续把模型能力落到真实业务流程中。

### 4. 为什么应聘 AI 应用开发工程师？

> 我希望做的是能够真正进入业务流程的 AI 应用，而不只是完成一个模型 Demo。我的项目经历覆盖了企业质量分析、代码成果评审、论文引用查证和 AI 辅助开发，能够接触数据接入、检索、工具、工作流、服务接口、评估和异常处理等完整环节。
>
> 这些项目让我认识到，AI 应用的难点不仅是模型能力，还包括数据是否可信、结论能否追溯、工具是否越权、任务能否恢复以及业务方是否能够接受。因此我希望在岗位中继续提升 AI 应用的效果评估、工程交付和业务落地能力。

---

# 二、项目一：质量管理体系 Agent

## 1. 主问题：请介绍一下质量管理体系 Agent

> 这个项目面向企业内部质量管理场景，接入禅道 BUG、GitLab Commit、飞书需求文档和质量体系文件，帮助研发和质量团队查询指标、分析质量问题，并得到有依据的回答。
>
> 我参与构建了基于 LangGraph 的“意图识别 → 受控工具调用 → 结果生成”工作流，并设计 ToolRegistry，将缺陷严重等级、功能分布、激活和需求变更等指标封装为可调用工具。结构化业务数据由工具和指标引擎计算，质量体系 checklist 通过 RAG 提供规范依据，LLM 主要负责理解问题和基于事实归纳。
>
> 针对中心系统不能主动访问分公司内网 GitLab 的问题，我参与实现 Edge 主动同步架构，结合 SQLite Outbox、游标增量同步、批次幂等接收、多实例数据隔离和 Docker 化交付，将分公司 Commit 数据安全接入中心质量看板。项目的核心思想是让不同类型的信息由合适的模块负责，降低无依据判断风险。

## 2. 追问：你在项目中具体负责什么？

> 按当前简历和已有材料，我可以确认自己负责或参与的部分包括：LangGraph 主流程构建、ToolRegistry 设计、结构化指标和 RAG 的职责边界设计、LLM 需求变更标注落库，以及 Edge 主动同步架构相关实现。
>
> 面试时我会重点展开自己实际编写、设计或排查过的模块，不把团队其他成员的工作全部归为个人完成。对于没有保留准确指标的部分，我会讲设计结果、验证方式和当前限制，不虚构线上数据。

## 3. 追问：为什么不把所有数据直接交给 RAG 或 LLM？

> BUG 数量、严重等级分布、功能分布等属于结构化事实，需要稳定、可复现和可审计的计算结果，应该交给数据库查询、指标引擎或受控工具完成。质量体系文件、流程规范和 checklist 属于文档知识，适合通过 RAG 检索原始条款。
>
> LLM 负责意图理解、参数组织和基于工具结果与检索依据的自然语言归纳。这样可以减少模型漏算、重复统计、口径不一致和幻觉数字。

### 继续追问：结构化数据转成文本放进向量库可以吗？

> 可以作为辅助检索，但不适合作为核心统计链路。文本化之后可能丢失字段类型、时间范围和统计口径，也不利于精确过滤和审计。更稳妥的方式是保留原始结构化数据，把可供解释的结果连同查询口径、时间范围和来源传给模型。

## 4. 追问：系统中的 RAG 具体承担什么职责？

> 在质量 Agent 中，RAG 主要检索质量体系文件、checklist 和相关规范，为回答提供条款依据。它不是用来计算 BUG 数量，也不是直接替代业务规则。
>
> 检索结果需要带来源、文档版本、章节或位置等元数据。若检索不到相关条款，回答应说明当前没有足够依据，而不是让模型根据常识补出一个规范结论。

### 继续追问：RAG 检索不到依据怎么办？

> 先检查查询改写、文档解析、切分、向量化、关键词匹配和元数据过滤是否存在问题。如果确认没有足够依据，就把已确认事实和未确认结论分开返回，并明确说明缺少哪份文档、哪个版本或哪类数据。高风险结论可以转人工确认。

## 5. 追问：LangGraph 工作流如何设计？为什么用它？

> 质量 Agent 的核心流程可以抽象为：请求接入、意图识别、参数校验、工具或 RAG 调用、结果汇总、回答生成和异常降级。LangGraph 适合把这些节点、状态和条件分支显式表达出来。
>
> 相比在业务代码中临时串联多个模型调用，LangGraph 更容易处理多步骤流程、状态传递、失败重试和人工确认。如果只是简单问答，我不会为了使用框架而强行设计复杂图。

### 继续追问：状态中应保存什么？

> 可以保存请求 ID、用户和组织上下文、原始问题、识别出的意图、结构化参数、工具调用记录、检索证据、生成结果、错误状态和人工确认结果。敏感内容需要按安全策略脱敏或限制持久化。

## 6. 追问：ToolRegistry 如何设计？

> 我会为每个工具定义名称、描述、输入 Schema、返回结构、权限要求、数据范围、错误语义、超时和是否允许副作用。质量 Agent 中，缺陷严重等级、功能分布、激活和需求变更等指标可以分别封装为工具。
>
> LLM 只负责选择和组织工具，服务端仍要进行身份认证、权限校验、参数校验、数据范围限制和执行审计。Prompt 不能替代服务端安全控制。

### 继续追问：为什么不能让 LLM 直接生成 SQL？

> 直接生成 SQL 容易产生字段理解错误、越权查询、全表扫描、统计口径错误和敏感数据泄露。更稳妥的方式是为固定业务指标提供受控工具，参数通过 Schema 和业务规则校验，数据库层再进行权限和资源限制。

## 7. 追问：意图识别错误有什么后果？怎么兜底？

> 意图识别错误可能导致错误工具调用、错误数据口径、无关文档检索，甚至越权访问。可以通过规则处理高确定性请求，用 Embedding 召回候选意图，再由 LLM 或分类器输出结构化结果，并结合参数和权限进行二次校验。
>
> 如果置信度不足、参数不完整或候选意图冲突，就向用户澄清；如果无法确认，则返回当前能确认的事实和限制，不能强行执行。

## 8. 追问：Edge 为什么要主动同步？SQLite Outbox、游标和幂等分别解决什么问题？

> 中心系统无法主动访问分公司内网，所以由分公司 Edge 在本地网络侧主动发起同步更符合网络边界。
>
> SQLite Outbox 用于先把待同步数据可靠写入本地，暂时断网时仍可等待后续发送；游标增量同步只传输上次位置之后的数据，减少重复；批次幂等接收用于处理网络重试和重复发送，保证同一批数据不会被重复写入。

### 继续追问：同步失败或进程重启怎么办？

> 失败时保留 Outbox 数据和同步状态，采用可控重试并记录错误；游标不能在数据可靠提交前提前推进。中心端通过批次 ID 或稳定幂等键去重。具体重试间隔、最大次数和告警阈值应根据实际部署配置，不能把未验证的参数当作项目现状。

## 9. 追问：如何保证不同分公司或实例数据不串？

> 请求、任务、数据库记录、缓存 Key、异步消息和日志都要携带组织或实例标识。服务端根据认证身份确定可访问范围，不能完全信任用户在请求中传入的租户 ID；数据库查询和工具执行都要强制增加隔离条件。
>
> 另外，异步任务恢复时也要恢复组织上下文，缓存不能使用不带租户信息的通用 Key，审计日志要能追溯到具体组织和任务。

## 10. 追问：需求变更标注为什么使用 LLM，还要落库？

> 缺陷历史记录中的需求变更原因通常需要语义理解，LLM 适合在明确标签集合和输出格式的前提下进行标注。但标注结果不应只保留一个标签，还应尽可能记录原始文本、依据、模型或规则版本和审核状态。
>
> 落库可以避免每次查询都重复判断，降低成本和延迟，也能保持结果一致、支持人工抽检和后续纠错。模型版本变化时可以按版本重新标注，而不是覆盖历史结论。

## 11. 追问：如何评估这个 Agent 的回答是否可靠？

> 至少分四层评估：第一是意图和参数是否正确；第二是工具返回的结构化事实是否符合数据口径；第三是 RAG 是否找到了足以支持结论的规范依据；第四是最终回答是否只使用了已有事实和引用。
>
> 可以建立典型质量问题的离线评估集，衡量意图路由、工具选择、参数正确率、指标结果、证据覆盖率和最终回答质量。线上再记录工具成功率、空结果率、降级率、人工修改或确认结果。当前没有完整线上指标时，我会明确说明评估框架和待补数据。

## 12. 追问：Agent 的安全风险有哪些？

> 主要风险包括自然语言诱导越权工具调用、模型生成非法或过大参数、RAG 文档中的 Prompt Injection、组织数据串读、日志泄露敏感信息，以及外部同步数据来源不可信。
>
> 对应控制包括服务端工具白名单、身份和权限校验、Schema 与业务范围校验、文档来源治理、组织维度数据隔离、日志脱敏、Edge 身份认证、批次幂等和资源限制。

## 13. 追问：如果新增一个数据源或质量指标，如何扩展？

> 先定义数据源的身份、字段、更新时间、可信度和权限边界，再设计独立适配层或同步层，避免把数据源细节直接写进 Agent 主流程。新的质量指标通过统一指标接口注册，由工具暴露给 Agent；回答层只消费统一结构化结果。
>
> 同时补充数据质量检查、权限隔离、评估样本、日志和异常处理，最后用回放样本验证新增指标不会破坏已有路由和回答。

---

# 三、项目二：功能开发成果评审 Agent

## 1. 主问题：请介绍功能开发成果评审 Agent

> 这个项目面向 GitLab 代码仓库，目标是根据固定 checklist 生成可追溯的功能开发成果评审草稿，并由人工确认后归档。
>
> 我参与设计和实现了 9 个阶段的 LangGraph 工作流，串联清单加载、仓库快照、目录与依赖关系解析、事实提取、证据组装和逐项裁决。核心设计是 Evidence-Grounded：每一项评审结论都必须关联文件路径、行号和快照 SHA 等证据，并对 LLM 输出做证据索引校验。无证据、引用非法或事实不足时，降级为“无法确认”，而不是直接判定不符合。
>
> 另外，我参与设计了任务状态机，覆盖创建、执行、待人工确认、取消、失败和服务重启中断等状态，并通过幂等创建、进度跟踪、取消/重试、审计记录、只读 GitLab Token、源码不落库、并发和文件大小限制控制风险。

## 2. 追问：为什么要先做快照、事实提取和证据组装，不能让 LLM 直接读代码？

> 直接让 LLM 阅读整个仓库并给结论，会面临上下文过长、版本不固定、遗漏关键文件、证据难定位和结论不可复查的问题。固定仓库快照能够保证评审对象稳定；目录和依赖解析帮助定位相关代码；事实提取把代码转成可验证事实；证据组装为每项 checklist 提供候选证据。
>
> 最后 LLM 只在受限事实和证据范围内归纳，减少自由发挥。

## 3. 追问：为什么绑定 `snapshot_sha`，而不是直接使用分支名？

> 分支名会随着提交变化，同一个分支在不同时间对应不同代码。绑定 `snapshot_sha` 可以固定评审时使用的代码版本，使文件路径、行号和最终结论能够复现。后续复核时也能确认结论到底基于哪个快照生成。

## 4. 追问：如何避免模型编造路径或行号？

> 先从固定快照中提取真实文件、行号和证据片段，形成候选证据集合；模型只能引用候选证据。生成后由服务端检查路径是否存在、行号是否有效、快照 SHA 是否一致，以及引用内容是否属于该文件。
>
> 如果校验失败，就将结论降级为“无法确认”，而不是相信模型的文字描述。

## 5. 追问：“不符合”和“无法确认”有什么区别？

> “不符合”表示已经找到明确证据证明 checklist 条件没有满足；“无法确认”表示当前快照、文件、权限或证据不足，不能得出结论。两者不能混淆，否则会把信息缺失误报成质量问题。

## 6. 追问：为什么是 9 个阶段？节点拆得太细有什么问题？

> 9 个阶段是从业务流程角度拆分的：清单加载、仓库快照、目录解析、依赖解析、事实提取、证据组装、逐项裁决、人工确认和归档等。这样每个阶段职责更清晰，便于定位错误、记录进度和局部重试。
>
> 节点也不能无限拆细，否则状态传递和维护成本会上升。拆分标准是阶段是否有独立输入输出、是否有独立失败处理、是否需要单独观测或人工介入。

## 7. 追问：任务状态机有哪些状态？

> 核心状态包括创建、执行、待人工确认、已完成、取消、失败和服务重启中断。状态转换必须经过合法性校验，例如已取消任务不能再次直接变成已完成，待人工确认任务不能绕过确认归档。

## 8. 追问：如何设计创建幂等？

> 客户端或服务端生成稳定的幂等键，中心记录幂等键与任务结果的对应关系。重复创建请求到达时，返回已有任务，而不是重新创建。幂等记录需要和任务创建处于同一事务或具备等价的一致性保证。

## 9. 追问：取消和任务完成同时发生，怎么避免状态错乱？

> 不能只依赖内存变量，而要使用数据库事务、版本号或条件更新保证只有一个合法状态转换成功。例如只有任务仍处于执行状态时，取消操作才能更新为已取消；完成操作也必须检查当前版本和状态。另一个操作失败后读取最终状态并返回真实结果。

## 10. 追问：服务重启时正在执行的任务怎么办？

> 服务重启后，不能假设内存中的任务还在正常执行。可以把没有最终状态的任务标记为中断，然后根据节点检查点判断是从最近阶段恢复、重新执行，还是进入人工处理。可重试节点要保证幂等，避免恢复时重复写入或重复产生副作用。

## 11. 追问：如何处理大仓库和慢请求？

> 先限制单次仓库大小、文件大小、并发数和允许分析的文件类型；再采用任务异步化、阶段化处理和进度记录，避免长时间占用同步请求。对目录解析、依赖解析和事实提取等阶段进行缓存或增量处理，但缓存必须绑定快照 SHA，不能把不同版本混在一起。

## 12. 追问：为什么源码不落库？审计如何保留？

> 源码可能包含企业敏感信息，完整落库会扩大泄露面和存储成本。因此可以只保留仓库标识、快照 SHA、文件路径、行号、证据摘要、评审结论、模型版本和操作记录。需要复核时基于原仓库权限访问对应快照。
>
> 具体保留哪些摘要需要结合安全和审计要求，不能为了审计而无边界保存源码。

## 13. 追问：为什么使用只读 GitLab Token？

> 评审 Agent 的职责是读取仓库并生成评审草稿，不应该具备写代码、改分支或发布的权限。使用只读 Token 能缩小权限范围，降低模型误调用或凭证泄露后的影响。高风险操作应由独立系统和人工审批完成。

## 14. 追问：什么是 Evidence-Grounded？

> Evidence-Grounded 是让每个结论都建立在可定位、可校验的证据上，而不是只要求模型回答得通顺。评审结论需要关联具体文件路径、行号、快照 SHA 和证据片段，并通过服务端校验引用合法性。
>
> 它不能保证模型绝对正确，但能把结论变成可复查对象，并在证据不足时安全降级。

## 15. 追问：如何评估评审 Agent 的可靠性？

> 可以按 checklist 项建立人工标注集，评估逐项结论准确率、证据覆盖率、引用合法率、误报率、漏报率和“无法确认”的合理性。工程上还要记录每个节点耗时、失败率、重试率、取消率和人工修改率。
>
> 评审 Agent 不是只看最终任务完成率，因为任务完成但证据错误、误报严重或耗时过长，仍然不能算可靠。

---

# 四、项目三：论文引用查证智能体

## 1. 主问题：请介绍论文引用查证智能体

> 这个项目使用 LLM 和 RAG 对论文中的引用进行查证，完成 PDF 解析、证据检索、引用合理性判断和结构化结果输出。
>
> 我使用 GROBID 解析论文 PDF 并提取 TEI XML，完成文本切分、向量化、FAISS 检索和元数据过滤；随后构建 BM25、FAISS 与 Rerank 结合的 Hybrid RAG 链路。论文解析、上下文检索和引用验证被封装为受控工具，通过 FastAPI 提供服务接口，并结合 Redis 缓存、Docker、日志和异常处理完成工程化交付。评估时使用人工标注集，关注 Recall@K、证据覆盖率和判定准确率；证据不足时返回“无法确认”。

## 2. 追问：为什么使用 GROBID？

> 论文 PDF 不是天然结构化文本，直接抽取容易丢失标题、段落、引用和参考文献之间的关系。GROBID 可以将论文解析成带结构的 TEI XML，便于后续按章节、段落和引用位置组织文本，减少纯文本解析带来的结构损失。

## 3. 追问：为什么使用 Hybrid RAG？

> 向量检索擅长语义相似，BM25 对专有名词、论文编号、作者名和精确短语更敏感。两者结合可以兼顾语义召回和关键词精确匹配，再通过 Rerank 对候选证据排序。
>
> Rerank 一般只处理初步召回的候选集合，不直接对全库排序，以平衡效果和成本。

## 4. 追问：Chunk 如何设计？

> 需要结合论文结构和引用查证目标，尽量按段落、章节和引用上下文切分，并保留标题、章节、页码、引用标识和文档 ID 等元数据。Chunk 太大，会引入无关内容并增加上下文成本；太小，则可能缺少完整论证关系。
>
> 最终要通过人工标注集比较不同切分策略对 Recall@K、证据覆盖率和判定准确率的影响。

## 5. 追问：如何判断检索到的证据真的支持结论？

> 不能只看相似度分数。需要检查证据是否属于正确论文和引用上下文，是否包含能够支撑结论的事实，是否与引用对象和语义关系一致。可以让模型输出结构化判断和依据，再通过规则或人工标注评估证据覆盖率与判定准确率。
>
> 证据不足时返回“无法确认”，不把高相似度片段直接等同于事实证明。

## 6. 追问：FAISS 和向量数据库有什么区别？

> FAISS 更偏向本地向量索引库，适合单机或原型场景，检索速度快、使用简单；完整向量数据库通常提供持久化、过滤、分布式、权限和运维能力。选择时要结合数据规模、并发、部署复杂度和过滤需求，不能只比较检索 API。

## 7. 追问：如何优化 RAG 效果？

> 我会按链路定位：先检查 PDF 解析是否丢结构，再检查 Chunk、元数据、Embedding、BM25、Top-K、Rerank 和最终证据使用情况。评估时分别记录召回指标、证据覆盖率和最终判定准确率，避免只凭主观感觉调整 Prompt 或更换模型。

## 8. 追问：论文查证项目如何工程化？

> 使用 FastAPI 对论文解析、检索和验证能力提供服务接口；Redis 用于缓存适合复用的中间结果；Docker 统一运行环境；日志和异常处理用于定位解析失败、检索失败和模型调用失败。服务设计上还要明确输入文件限制、超时、重复任务处理和证据不足的返回语义。

---

# 五、项目四：AI Coding Harness 与 Context Engineering

## 1. 主问题：AI Coding Harness 是什么？

> 我自建了一个面向 Claude Code 和 Codex 的最小 AI Coding Harness。它不是另一个自动写代码的 Agent，而是为 AI 编程工具提供受控的开发上下文和人工审批流程。
>
> 我把架构约束、接口契约、测试策略、稳定决策和任务状态沉淀为 `dev_tasks`、`decision_log`、`api_contract`、`test_plan` 等协作机制，形成“业务建模 → 任务单人工确认 → AI 编码与测试 → 人工审查验收 → 归档续接”的闭环。这样可以减少 AI 上下文漂移、误改无关模块和绕过业务责任的问题。

## 2. 追问：为什么不能把整个仓库都交给 AI？

> 仓库越大不代表上下文越好。把全部代码、历史对话和文档放进上下文，会增加 Token 成本和噪声，关键约束容易被淹没，旧事实也可能与新代码混在一起。
>
> 更有效的方式是先固定任务范围和版本，再加载架构、决策、接口契约和测试计划，最后按需检索入口、调用链、数据模型和相关测试。信息仍不足时应升级给人，不能让模型凭常识补全。

## 3. 追问：什么是 Context Engineering？

> Context Engineering 是系统性地设计 Agent 在每个任务阶段能够看到什么信息、信息来自哪里、如何组织、什么时候更新以及哪些内容不能被覆盖。它不只是写一个更长的 Prompt。
>
> 在 Repo 级开发中，模型至少要知道改什么、为什么改、不能碰什么、谁依赖它以及如何验证。对应到 Harness，就是任务单、架构文档、决策记录、接口契约、测试计划和任务状态共同构成可复用上下文。

## 4. 追问：如何做上下文压缩？

> 先保留硬约束、接口契约、权限、安全边界、验收标准和当前任务相关代码；再把历史对话压缩成已确认事实、稳定决策、未解决问题和待办事项；对已经完成的日志和重复信息做摘要；遇到新问题时再按需检索。
>
> 目标不是单纯减少 Token，而是在降低成本的同时保持工具选择、参数、约束遵守和结果质量。

## 5. 追问：什么是 Skill？和普通 Function Call 有什么区别？

> Function Call 通常描述一次具体函数调用，包括函数名称、参数 Schema 和返回结果。Skill 是面向一类任务的可复用能力包，除了工具，还可以包含任务流程、领域知识、输入输出规范、示例、失败处理和安全边界。
>
> 例如一次查询严重缺陷数量是 Function Call；“质量分析 Skill”则可能包含意图识别、指标工具、需求变更分析、质量规范检索和结果汇总。

## 6. 追问：Skill 的渐进式披露是什么？为什么提高稳定性？

> 渐进式披露是先只暴露 Skill 的名称、用途和适用场景；任务匹配后加载输入输出、前置条件和限制；真正执行时再加载详细规则、示例和工具 Schema。
>
> 这样可以减少上下文噪声，避免相似 Skill 互相干扰，降低错误路由和误调用，也能让权限、版本和提示词管理更清晰。前提是索引和按需加载机制本身足够可靠。

## 7. 追问：你如何理解 Vibe Coding？

> Vibe Coding 是通过自然语言让 AI 参与代码生成、修改、测试和调试的开发方式，适合原型、样板代码、测试生成和重复性重构。
>
> 但它不能替代需求确认、架构设计、权限校验、测试和人工代码审查。我的 Harness 试图把 Vibe Coding 放进受控流程：先确认任务范围和约束，再让 AI 在限定范围内工作，最后由人审查验收。

## 8. 追问：Harness 和成果评审 Agent 有什么关系？

> Harness 主要解决开发前和开发中的问题：让 AI 知道任务目标、架构约束、接口契约和测试标准，并限制变更边界。成果评审 Agent 主要解决开发成果形成后的问题：对固定仓库快照提取事实、组装证据并生成可人工确认的评审草稿。
>
> 前者控制“AI 如何安全地做开发”，后者控制“AI 如何有依据地评审成果”，两者可以通过任务记录、决策记录、测试结果和评审证据形成闭环，但不是同一个项目。

## 9. 追问：AI 生成代码后为什么仍需要测试和人工确认？

> 测试只能证明被覆盖的行为满足预期，不能完全证明业务规则、权限边界、架构取舍和用户体验正确。人工确认负责判断需求是否理解正确、变更是否越界、接口是否兼容以及风险是否可接受。
>
> 对修改代码、写数据库、发布内容等高风险动作，人工确认更不能被模型自行替代。

---

# 六、通用 Agent、LLM 与 RAG 技术问题

## 1. Agent 和普通 LLM 问答有什么区别？

> 普通 LLM 问答通常是一次输入、一次生成；Agent 则围绕目标进行意图理解、任务规划、工具调用、状态管理、结果验证和必要的重试或人工确认。Agent 不一定越自主越好，企业场景更重要的是流程可控、权限明确、证据可追溯和失败可恢复。

## 2. Workflow、ReAct、Plan-and-Execute 如何选择？

> Workflow 适合步骤和边界比较明确的业务流程，稳定性和可观测性较好；ReAct 适合工具选择具有动态性的探索任务，但执行轨迹更难控制；Plan-and-Execute 适合先规划再执行的复杂任务，但需要处理计划错误、计划过时和中途失败。
>
> 我的质量 Agent 和成果评审 Agent 更偏受控 Workflow，因为任务步骤、工具边界和人工确认点比较明确。

## 3. 如何降低 LLM 幻觉？

> 第一，结构化数据通过工具计算；第二，规范性结论必须引用 RAG 证据；第三，使用结构化输出 Schema；第四，对路径、行号、引用和工具结果做服务端校验；第五，证据不足时返回无法确认；第六，对高风险结果保留人工审批；第七，通过离线评估集和失败样本做回归。
>
> 降低 temperature 只能作为辅助，不能替代数据来源、证据校验和权限控制。

## 4. Agent 如何做意图识别？

> 可以组合规则、Embedding 候选召回、LLM 结构化分类和参数校验。意图识别不能只输出一个类别，还要判断参数是否完整、工具是否可用、用户是否有权限和是否涉及高风险动作。
>
> 如果一个问题同时包含多个意图，需要拆成带依赖关系的子任务；如果意图冲突或置信度不足，应向用户澄清。

## 5. 一次输入多个诉求时，如何拆分任务？

> 先识别任务边界和依赖关系，再为每个子任务分配独立 ID、输入、输出和错误状态。共享的时间范围、项目范围、版本和权限上下文必须统一；有依赖的任务按顺序执行，互不依赖的任务才考虑并行。
>
> 最终结果按子任务汇总，明确哪些完成、哪些失败、哪些证据不足和哪些需要人工确认，避免一个任务的结果覆盖另一个任务。

## 6. 多 Agent 之间如何分配模型？

> 根据任务复杂度、上下文长度、风险等级、延迟要求、并发量、成本和模型评估表现进行路由。简单分类和格式转换可以使用小模型，复杂规划和高风险分析使用能力更强的模型，确定性统计优先用工具。
>
> 不一定全部使用一个大模型，因为成本、延迟和吞吐不划算，而且大模型也不适合替代确定性计算。但模型拆分过多会增加路由复杂度，应通过离线评估确认收益。

## 7. 语音、文本和工具结果同时到达时，状态机怎么设计？

> 这不是我的项目经历，因此我不会把它说成已实现能力。作为通用设计，我会把不同模态统一成带时间戳、会话 ID、任务 ID 和序列号的事件，再由状态机推进。
>
> 语音 partial result 不能直接触发高风险动作，必须等待 final result；工具结果要关联调用 ID；状态更新需要版本控制或条件更新，防止旧事件覆盖新状态；同时支持取消、超时、重试和服务重启恢复。

## 8. 如何判断 Agent 是否生产可用？

> Demo 只证明一条理想路径可以跑通；生产可用还需要证明真实输入、数据缺失、工具失败、权限约束、并发、服务重启和模型变化下系统仍然可控。
>
> 我会检查业务边界、证据和数据来源、工具权限、状态持久化、幂等、取消、重试、恢复、评估回归、链路追踪、日志脱敏、成本和维护能力。当前项目中已经重点设计了工具边界、证据校验、状态机、幂等、数据隔离和人工确认；线上规模指标和完整故障演练需要以实际部署证据为准。

## 9. Agent 体系应该如何评估？

> 评估应覆盖输入理解、工具使用、检索证据、最终回答和工程业务指标。可以分别衡量意图准确率、参数正确率、工具选择准确率、工具成功率、Recall@K、证据覆盖率、引用正确率、回答事实正确性、降级合理性、延迟、成本、人工修改率和异常率。
>
> 任务成功率不能作为唯一标准，因为任务可能完成但证据错误、越权、成本过高或高风险样本失败。

## 10. 如何做 Prompt Injection 防护？

> 首先把外部文档、用户输入和系统指令分层，不把检索内容当成高优先级指令；工具执行采用服务端白名单、Schema 校验和权限校验；限制敏感工具、文件、网络和命令访问；对高风险动作增加人工确认；记录异常输入并加入安全评估集。
>
> Prompt 约束只能辅助，不能替代服务端的身份认证、数据隔离和执行权限。

---

# 七、工程基础与模型基础问题

## 1. FastAPI 在你的项目中承担什么角色？

> FastAPI 主要用于提供 Agent 服务接口，包括请求接入、任务创建、状态查询、结果返回和异常处理。Pydantic 用于请求和响应 Schema 校验。长时间任务更适合先创建任务并返回任务 ID，再异步执行和查询进度，避免同步请求长时间阻塞。

## 2. 如何设计长任务的取消、重试和恢复？

> 为任务定义明确状态和唯一 ID；节点执行前后记录状态和错误；取消通过持久化标记或条件状态转换实现；临时错误才重试，永久错误直接失败；服务重启后把未结束任务标记为中断，并基于检查点恢复或重新执行。所有可能产生副作用的操作必须具备幂等控制。

## 3. 你如何做日志和链路追踪？

> 记录 `request_id`、`session_id`、`task_id`、组织标识、模型版本、Prompt 版本、工作流版本、节点名称、工具名称、参数摘要、检索元数据、耗时、Token 使用、错误类型和最终状态。
>
> 同时要做日志脱敏，不能把 Token、源码、个人信息和敏感业务数据完整写入日志。监控可以关注延迟、失败率、工具成功率、空结果率、降级率、重试率、人工修改率和成本。

## 4. Docker 化交付解决什么问题？

> Docker 可以统一运行环境、依赖版本和启动方式，减少本地与部署环境差异，也便于隔离服务组件和交付。但 Docker 本身不等于生产高可用，还需要结合资源限制、健康检查、日志、配置管理和实际部署平台。

## 5. MySQL、Redis 在 AI 应用中分别适合做什么？

> MySQL 适合持久化任务、用户、业务数据、评审结果和审计记录，强调事务和一致性；Redis 适合缓存检索结果、会话短状态、分布式锁或限流信息，强调低延迟。
>
> 缓存必须考虑过期、失效、数据更新和租户隔离，不能把 Redis 当作最终事实来源。

## 6. 你如何理解 Transformer、Embedding 和 Rerank？

> Transformer 通过 Self-Attention 建模序列中不同位置之间的关系。Embedding 把文本映射为向量，适合进行语义相似度召回；Rerank 对初步召回的候选结果进行更精细的相关性排序。三者在 RAG 中职责不同，不能把 Embedding 相似度直接当成最终答案正确性。

## 7. 你使用 PyTorch 的经验如何？

> 我的深度学习实践主要来自研究生课题，使用 ResNet-18 提取视频帧空间特征，再使用 LSTM 建模时间序列，完成数据划分、训练和评估流程；同时设计了红外增强、Uniform Sampling 和轻量 Temporal Attention。
>
> 我的优势是具备完整的模型训练和评估基础，并能将算法与业务场景结合；大规模分布式训练、强化学习训练框架不是当前简历中可以直接证明的经历，我会如实说明。

## 8. 为什么使用 ResNet-18 + LSTM，而不是 Transformer？

> ResNet-18 结构成熟、相对轻量，适合提取单帧空间特征；LSTM 能够建模视频帧序列中的时间依赖。当前课题明确构建的是非 Transformer 基线，便于控制复杂度并分析红外增强、采样和时间注意力的作用。
>
> Transformer 具有更强的全局时序建模能力，但可能带来更高计算和数据需求。是否更优需要在相同数据协议和指标下实验比较，不能直接宣称未实现方案更好。

## 9. Accuracy、AUC 和 F1 有什么区别？

> Accuracy 表示整体预测正确比例；AUC 反映不同阈值下对正负样本的区分能力；F1 是 Precision 和 Recall 的调和平均，适合同时关注误检和漏检的场景。
>
> 三者不能直接横向比较大小，必须固定数据划分、类别定义、阈值和实验配置。我的课题中使用这些指标，是因为动作触发场景既要关注整体识别，也要关注误检和漏检平衡。

---

# 八、全栈与实时交互项目

## 1. 请介绍轻舟货运飞船前端项目

> 这是面向航天领域地面站的实时遥测监控与货物辅助管理前端系统。我使用 Vue3 构建遥测数据表格与趋势图、摄像机预览、灯光控制、报警监控、货架可视化和操作记录等模块。
>
> 我实现了遥测、报警、货物状态和语音事件的周期性刷新机制，通过事件去重、全局路由跳转和状态管理保障页面一致性；使用 ECharts 展示遥测趋势；针对用户正在操作时可能被刷新数据覆盖的问题，设计了交互期间暂停、完成后恢复刷新；还使用运行时配置解耦不同部署环境的后端地址。

## 2. 追问：如何处理实时刷新和用户操作冲突？

> 不能让周期刷新无条件覆盖用户正在编辑或控制的状态。我会区分服务端事实、页面展示状态和用户临时操作状态，在交互期间暂停相关刷新，操作完成后重新拉取最新数据；同时通过事件去重和统一状态管理避免同一事件重复触发。

## 3. 追问：为什么没有直接使用 WebSocket？

> 当前简历确认的是周期性刷新，不应把项目说成已经使用 WebSocket。周期刷新实现简单，适合更新频率不高或后端暂不支持长连接的场景；如果实时性要求更高，可以评估 WebSocket 或 SSE，但需要同时考虑连接管理、断线重连、消息顺序、重复事件和服务端推送压力。

## 4. 追问：你有传统全栈开发经验吗？

> 我参与过 SaaS 系统“健身助手”的研发与维护，并主导“剧帮主”产品从 0 到 1 的开发，涉及 PC 端、小程序端以及 PHP/MySQL 后端，包含预约、会员、公众号推送、支付和第三方电子签名合同等功能。这些经历让我熟悉业务拆解、多端协同、数据库和第三方接口集成。
>
> 目前我的主力方向已经转向 Python AI 应用和 Agent，但传统全栈经历帮助我更好地理解前后端接口、用户流程和业务交付。

---

# 九、岗位要求与风险问题

## 1. 你会 Python、Java、C++ 到什么程度？

> 我的主力工程语言是 Python，AI 应用和后端项目主要使用 Python、FastAPI、SQLAlchemy 等技术栈。C++ 在本科阶段学习和使用过，具备数据结构、算法和基础工程理解，但目前不是主要生产开发语言。Java 不是我当前最熟练的语言。
>
> 我不会把自己包装成 Java 或 C++ 方向的资深开发，但已有编程、接口设计、数据库和工程经验，能够根据岗位需要补齐。面试时应继续展开自己熟悉的 Python 和项目代码，而不是主动夸大其他语言经验。

## 2. 你使用过 TensorFlow、PyTorch 或主流 AI 框架吗？

> 我的深度学习实践主要使用 PyTorch 相关训练流程，来自红外视频动作识别课题。主流大模型应用框架方面，我在实际项目中主要使用 LangChain 和 LangGraph。
>
> 对其他框架，我会先理解模型接入、Prompt、工具、Workflow、Memory、RAG 和部署这些共同抽象，再通过官方文档和最小 Demo 上手。对于没有实际使用过的框架，我会说“了解或正在学习”，不会说成生产经验。

## 3. 如果面试官问到你没做过的 ASR 项目，怎么回答？

> ASR 不是我的项目经历，我不会把其他人的面经或截图内容说成自己的经验。可以回答：“我没有实际做过 ASR 项目，因此不能从线上实现角度展开。不过从 AI 应用设计角度，我知道需要关注准确率、关键词召回、首字延迟、最终结果延迟、噪声和断线恢复；如果岗位涉及该方向，我会先从数据、评估指标和实时链路入手学习。”

## 4. 如何处理“简历中的技术是不是都你做的？”

> 我会按模块说明自己的职责。例如质量 Agent 中，我负责或参与 LangGraph 主流程、ToolRegistry、结构化数据与 RAG 边界、需求变更标注和 Edge 同步相关工作；成果评审 Agent 中，我负责或参与工作流、证据链校验和任务状态机等内容。
>
> 对团队其他成员负责的模块，我会明确说“团队完成”或“我参与”。不确定的细节不临场补充。

## 5. 项目没有量化线上指标，怎么证明效果？

> 可以从可观察结果和验证方法说明：是否减少人工跨系统对照、是否能提供数据和证据、是否支持失败降级、是否支持取消/重试/恢复、是否能通过人工标注集评估、是否能通过日志定位问题。
>
> 如果没有正式线上指标，就直接说明当前缺口，并提出后续需要补充的请求量、延迟、错误率、工具成功率、证据覆盖率、人工修改率和成本等指标。

## 6. AI 应用从需求到上线如何推进？

> 我会先明确用户、业务目标、输入输出和错误代价，再拆分 AI 能做和不应该做的部分；确认数据来源、权限和隐私边界；选择 RAG、工具、Workflow 或普通接口；先做最小原型；建立离线样本和验收标准；补充日志、异常、权限和安全测试；小范围试用后根据人工反馈迭代，最后再扩大使用范围。
>
> 我不会一开始只讨论使用哪个模型，而是先判断业务问题、数据和评估标准是否明确。

## 7. 如何与产品、数据和算法团队协作？

> 和产品沟通时重点确认业务目标、用户流程、边界和验收标准；和数据团队确认数据来源、字段含义、质量、更新频率和权限；和算法团队确认模型能力、输入输出、评测结果和已知限制；和后端或部署团队确认接口、资源、日志和发布方式。
>
> 遇到分歧时，我会把问题拆成事实、约束、方案和验证方式，通过小范围实验或评估数据推动决策，而不是只讨论个人偏好。

## 8. 你认为自己当前的不足是什么？

> 我的主要实践优势集中在 Python AI 应用、RAG、Agent 工作流和后端服务，Java、复杂云原生平台、分布式高并发和大规模模型训练不是当前最强项。对此我会通过补齐计算机基础、服务部署、观测和评测体系来扩大工程深度。
>
> 我的优势是已经形成了比较清晰的系统设计方式：先明确数据和责任边界，再设计工作流、工具、证据和降级，而不是只追求模型 Demo 的效果。

---

# 十、行为面试问题

## 1. 为什么从传统开发转向 AI 应用和 Agent？

> 我之前有 Vue、PHP、MySQL、第三方接口和业务系统开发经历，后来在“AI 流量盒子”项目中接触到 OpenAI API、Prompt 和内容生成。研究生阶段又接触深度学习。实习后我进一步参与企业质量管理和代码评审 Agent，发现自己更感兴趣的是如何把模型能力接入真实业务，并通过工具、RAG、状态机和人工确认把它做得可控。因此我希望继续沿 AI 应用工程方向发展。

## 2. 你最大的技术优势是什么？

> 我比较擅长做责任边界划分。结构化事实交给工具和指标计算，规范知识通过 RAG 提供依据，LLM 负责意图理解和受约束归纳，高风险场景保留人工确认。同时我有 Python 后端、前端和传统全栈经历，能够把模型链路连接到真实接口和业务流程。

## 3. 讲一次你解决复杂技术问题的经历

> 可以讲分公司 GitLab 跨网络接入：中心系统无法主动访问内网，因此采用 Edge 主动同步；本地用 SQLite Outbox 保存待同步数据，用游标做增量，用批次幂等处理重试，并结合实例隔离和 Docker 交付。这个问题让我认识到，企业 Agent 不只有模型和工作流，还要处理网络边界、可靠传递和部署隔离。

## 4. 讲一次你给 AI 工具设定边界的经历

> 可以讲 AI Coding Harness：直接让 Claude Code 或 Codex 自由修改仓库，容易上下文漂移、违反架构和接口约束。因此我把任务、架构、决策、接口和测试结构化，并加入任务单人工确认、限定范围编码、人审验收和归档续接，使 AI 提升效率但不越过业务责任边界。

## 5. 遇到需求变化时如何处理？

> 先确认变化是目标变化、范围变化还是实现变化，再判断对数据、接口、工作流、测试和交付时间的影响。把变化记录为新的任务或决策，更新接口契约和测试计划，不能只在 Prompt 中临时修改。对于不明确的业务规则，先让产品或业务确认。

## 6. 遇到模型输出错误或证据不足怎么办？

> 先定位错误发生在意图、工具参数、数据、检索、证据组装还是生成环节。对于证据不足，不能用模型常识补全，而是返回已确认事实和缺少的信息；对于高风险结论，交人工确认；把错误样本加入离线评估集，避免只修改一个 Prompt 后问题再次出现。

## 7. 如果团队成员质疑使用 RAG 或 LangGraph 过度设计，你怎么回应？

> 我会先判断业务是否真的需要它。简单问答不需要复杂工作流；如果任务涉及多步骤工具调用、状态、证据、重试或人工确认，显式工作流和检索依据能降低维护和审计成本。最终用真实样例、错误率、延迟和开发成本比较，而不是单纯坚持某个框架。

---

# 十一、面试前重点背诵清单

## 必须熟练回答的 15 题

1. 一分钟自我介绍。
2. 为什么应聘 AI 应用开发工程师？
3. 质量管理体系 Agent 的完整架构。
4. 你在质量 Agent 中具体负责什么？
5. 为什么结构化数据不用 RAG？
6. ToolRegistry 如何设计以及如何防越权？
7. LangGraph 为什么适合当前项目？
8. 成果评审 Agent 的 9 个阶段是什么？
9. 如何避免模型编造文件路径和行号？
10. 任务状态机如何实现取消、重试和恢复？
11. 论文查证 Agent 的 Hybrid RAG 链路。
12. 如何评估 RAG 和 Agent 的效果？
13. AI Coding Harness 和 Vibe Coding 的关系。
14. 如何判断 Agent 是否生产可用？
15. Java/C++、PyTorch 和大模型框架的真实掌握边界。

## 现场画图题

### 质量管理 Agent

```text
用户
→ FastAPI
→ 意图识别与参数校验
→ LangGraph
→ ToolRegistry / RAG
→ 结构化事实与证据汇总
→ LLM 受约束生成
→ 结果、依据、降级信息和审计
```

### 成果评审 Agent

```text
创建任务
→ 加载 checklist
→ 固定仓库快照
→ 目录/依赖解析
→ 事实提取
→ 证据组装
→ 逐项裁决
→ 人工确认
→ 归档
```

### 论文引用查证 Agent

```text
PDF
→ GROBID / TEI XML
→ 文本切分与元数据
→ BM25 + FAISS
→ Rerank
→ 证据组装
→ 引用判断
→ 结构化结果 / 无法确认
```

### AI Coding Harness

```text
业务建模
→ 任务单人工确认
→ 读取架构/决策/接口/测试上下文
→ AI 编码与局部测试
→ 人工审查验收
→ 归档与下一轮续接
```

## 统一答题模板

回答项目问题时按以下顺序：

1. 背景与业务问题；
2. 目标和约束；
3. 总体架构；
4. 核心技术决策；
5. 本人职责；
6. 难点和解决方式；
7. 结果和验证方法；
8. 当前限制和后续改进。

## 事实边界提醒

- ASR 不是我的项目，不说做过流式或非流式 ASR。
- 不把他人面经中的项目、技术和指标说成自己的经历。
- 不把 Agent 设计机制说成已经获得线上 QPS、准确率或用户规模指标。
- 不把团队成果全部归为个人完成。
- 不把“无法确认”说成“不符合”。
- 不把当前红外动作识别研究说成已经完成完整多模态感知系统。
- 不把周期刷新说成已经使用 WebSocket。
- 不把了解过的 Java、TensorFlow、MCP 或其他框架说成生产经验。
- 论文查证项目的指标必须结合真实实验记录，不临场扩展样本数量、硬件和对比结论。

## 复习方法

### 第一次：项目主线

每个项目用 90 秒讲清背景、架构、职责、难点和边界。

### 第二次：随机追问

从每个项目随机抽 3 个追问，要求先给结论，再补技术细节。

### 第三次：白板表达

不看文档画出质量 Agent、成果评审 Agent 和论文查证 Agent 的数据流，说明每个模块为什么存在。

### 第四次：风险复盘

重点检查是否出现以下表达：

- 把“参与”说成“独立完成”；
- 把“设计方案”说成“线上验证结果”；
- 把“证据不足”说成“业务不合规”；
- 把其他岗位面经中的 ASR 内容套到自己身上；
- 只讲模型和框架，没有讲业务价值、权限、异常和评估。
